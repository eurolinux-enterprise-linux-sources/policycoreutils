From 1ab804040ef1abe680fb094f3dcbceddd9033d78 Mon Sep 17 00:00:00 2001
From: Petr Lautrbach <plautrba@redhat.com>
Date: Thu, 7 Jan 2016 16:16:31 +0100
Subject: [PATCH] sandbox: better document -C option

Resolves: rhbz#1091120
---
 sandbox/sandbox.8 | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/sandbox/sandbox.8 b/sandbox/sandbox.8
index a3eaf12..dd370e6 100644
--- a/sandbox/sandbox.8
+++ b/sandbox/sandbox.8
@@ -64,7 +64,7 @@ $HOME and /tmp, secondary Xserver, defaults to sandbox_x_t
 Set the DPI value for the sanbox X Server. Defaults to the current X Sever DPI.
 .TP
 \fB\-C, --capabilities\fR
-Use capabilities within the sandbox.  By default applications executed within the sandbox will not be allowed to use capabilities (setuid apps), with the -C flag, you can use programs requiring capabilities.
+Use capabilities within the sandbox.  By default applications executed within the sandbox will not be allowed to use capabilities (setuid apps), with the -C flag, programs can gain capabilities attached to executable files.
 .PP
 .SH "SEE ALSO"
 .TP
-- 
1.8.3.1

